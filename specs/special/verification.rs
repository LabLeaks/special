/**
@spec SPECIAL.VERIFICATION.COLOCATED_CONTRACTS
special should make verification contracts locally inspectable so a reviewer can validate the contract without chasing multiple files.

@planned
*/

/**
@spec SPECIAL.VERIFICATION.COLOCATED_CONTRACTS.SINGLE_REVIEW_SURFACE
special should prefer verification designs where the important contract can be understood from one local review surface instead of being split across spec prose, helper code, and tool config.

@planned
*/

/**
@spec SPECIAL.VERIFICATION.DRIFT_WARNINGS
special should warn when verification artifacts rely on external or ambient inputs that can silently drift.

@planned
*/

/**
@spec SPECIAL.VERIFICATION.DRIFT_WARNINGS.UNDECLARED_EXTERNAL_INPUTS
special should warn when a verification artifact depends on undeclared external tools, configuration, environment variables, or shell features.

@planned
*/
